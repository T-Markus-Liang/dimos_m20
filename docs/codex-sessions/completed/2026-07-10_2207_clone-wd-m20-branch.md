# Clone WD M20 Branch

## Metadata

- Date: 2026-07-10 22:07 CST
- Session id: current Codex desktop thread
- Project: dimos-wd-m20
- Workspace: `/home/markus/work/dimos_wd_m20` on VM `autoware-180`
- Task: clone the internally tested `MeloLong/dimos` `feat/wd/m20` branch into a separate VM workspace
- Status: completed
- Branch if relevant: `feat/wd/m20`

## User Request Summary

Create `/home/markus/work/dimos_wd_m20` in the Ubuntu VM and clone `https://github.com/MeloLong/dimos/tree/feat/wd/m20` into it without modifying the existing `/home/markus/work/dimos_m20` workspace.

## Work Done

- Connected directly to the VM with the key `/Users/markus/macos-vm/vm/ssh/autoware-1.8.0-ubuntu22-arm64_ed25519`.
- Confirmed the remote branch `refs/heads/feat/wd/m20` exists at commit `98713d97341bc9392e5dd531b1c13aaf98782a23`.
- Cloned with `GIT_LFS_SKIP_SMUDGE=1`, `--branch feat/wd/m20`, and `--single-branch` into `/home/markus/work/dimos_wd_m20`.
- Verified the checked-out branch, commit, origin remote, clean status, LFS pointers, and directory size.

## Decisions

- Skipped automatic Git LFS downloads to avoid pulling large assets during source checkout. The repository contains 193 tracked LFS files whose pointers remain available for selective download.
- Kept this checkout independent from `/home/markus/work/dimos_m20` so the tested WD branch can be evaluated without disturbing the existing Ivan/Dan integration workspace.

## Current State

- Branch: `feat/wd/m20`
- HEAD: `98713d97341bc9392e5dd531b1c13aaf98782a23`
- Origin: `https://github.com/MeloLong/dimos.git`
- Git status: tracking `origin/feat/wd/m20`; the only untracked content is the local `docs/codex-sessions/` log created after clone verification
- Checkout size: approximately 71MB
- LFS files: 193 pointers; large LFS content not downloaded

## Resume Instructions

1. Connect to the VM using the configured SSH key or alias.
2. Enter `/home/markus/work/dimos_wd_m20`.
3. Read this log and inspect `README.md`, `AGENTS.md`, `pyproject.toml`, and the M20 blueprint files before environment setup or comparison work.
4. Pull individual LFS assets only when a concrete test requires them.

## Open Questions

None for the clone task.
